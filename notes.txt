Currently functional as set of methods called separately.

Implement game method to run as a game. 

Requires rewriting of the tests and code, which currently put out their own text return values,
and so interfere with the flow.